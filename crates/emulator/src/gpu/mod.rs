
mod filter;

pub struct Gpu {
}

impl Gpu {

    pub fn new() -> Self {
        let mut gpu = Self {
        };

        // Return object ready to run
        gpu.reset();
        gpu
    }

    pub fn reset(&mut self) {

    }

    pub 

    
}
